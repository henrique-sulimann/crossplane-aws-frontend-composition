apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: function-aws-frontend
spec:
  compositeTypeRef:
    apiVersion: aws.sulimann.com.br/v1
    kind: Frontend
  mode: Pipeline
  pipeline:
  - step: run-the-template
    functionRef:
      name: function-aws-frontend
